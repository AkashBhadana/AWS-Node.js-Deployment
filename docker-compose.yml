version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    # To enable live-reloading during development, uncomment the volumes section below:
    # volumes:
    #   - .:/usr/src/app
    #   - /usr/src/app/node_modules
